#include <iostream>
#include <vector>

struct item {
	int value;
	int weight;
	item(int a, int b) : value(a), weight(b) {};
};

int knapSack01(const std::vector<item>& items) {

	return 0;
}

int main() {

	return 0;
}